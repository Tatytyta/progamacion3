document.addEventListener("DOMContentLoaded",() =>{
    console.log(document.head);      // <head> del documento
    console.log(document.children);  // <html> del documento
    console.log(document.title);     // muestra 'document object' model -> 'DOM'
});